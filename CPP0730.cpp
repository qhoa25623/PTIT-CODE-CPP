#include <bits/stdc++.h>
using namespace std;

using ll=long long;

int a[1000001];

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        for (int i = 0; i < n; i++)
        {
            cin >> a[i];
        }
        ll ans=INT_MIN;
        int idx=0,pos=0;

    }
    return 0;
}